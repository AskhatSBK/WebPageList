<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>SE-2323 Database</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <style>
        a{
            float: right;    
        }
    </style>
</head>
<body>
        <p class="h1 text-center">SE-2323 Group list</p>
    <ul class="list-group w-50 mx-auto mt-4">
        <% users.forEach(user =>{ %>
            <li class="list-group-item">
                <strong><%= user.name %></strong>: <%= user.age %>
                (<%= user.gender ? 'Male': 'Female' %>)
                <a href="/delete/<%= user.id %>" class="btn btn-danger btn-sm ms-3">Delete</a>
                <a href="/edit/<%= user.id %>" class="btn btn-warning btn-sm ms-3">Edit</a>
            </li>
        <% }); %>
    </ul>
    <a href="/add" style="-ms-flex-align: center;" class="btn btn-success btn-sm ms-3">Add New Student</a>
</body>
</html>